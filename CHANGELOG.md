# 更新记录

## 0.6.1 — 2026-09-20

- 自动识别误填在兼容服务字段中的百炼官方地址与 `fun-asr` / `paraformer-v2`，切换到百炼录音接口并带入同一密钥；不覆盖独立配置的另一套凭据，不修改其他服务。
- 原生 App 自检增加服务选择控件检查，避免只核对包版本而漏掉实际界面。
- 两种模型均已用同一段 45 秒课堂录音完成上传、识别与 SRT 输出验证；短样本通过不代表整课准确率保证。

## 0.6.0 — 2026-09-20

- 新增阿里云百炼录音文件识别，支持 `fun-asr` 和 `paraformer-v2`，自动上传私有临时音频、提交任务、查询结果。
- 根据服务返回的句子与词时间戳生成同名 SRT；长句仅在完整词时间戳可用时拆成字幕，保留英文词间空格。Paraformer 启用时间戳校准。
- 设置页新增服务选择和两种百炼模型，百炼与兼容服务的密钥分别存入钥匙串；切换服务保留原配置。
- 保存已提交的任务编号及已获取的结果；中断、查询失败或超时后继续查询，提交响应丢失时不会自动重复提交付费任务。
- 保留已有兼容服务缓存和课程产物，不引入本地模型；笔记仍用完整原文一次生成。
- 新增两个模型的离线 HTTP 合约、字幕时间轴、密钥隔离、恢复与界面测试。尚未用真实百炼账户验收识别效果。

## 0.5.2 — 2026-09-18

- 修复反相立体声录音在转为单声道时抵消讲话的问题：先保留原声道，抽样检查抵消，必要时选择有效声道；正常立体声仍合并两边内容。
- 音频预处理版本纳入块缓存标识，避免复用旧转换结果；已完成的整课转录和笔记继续保留。
- 学校提示“视频未到开放时间”时标为等待开放，不再反复重试或计为运行失败。
- 修复输出 tokens 预算被误当作密钥隐藏的问题，真实凭据继续脱敏。
- 云端转录日志显示每个新音频块及空结果提醒，等待期间不重复刷屏；增加声道、延迟开放、日志和脱敏回归测试。

## 0.5.1 — 2026-09-17

- 保存大文件校验缓存，跨进程复用未变化的视频；文件信息变化时重新校验，避免每次启动都从外置磁盘完整读取全部录像。
- 保留已完成笔记中的手动修改，继续核对其原始转录与来源；普通运行不因笔记正文被编辑而重新调用模型。
- 防止滚动设置页时误改数值与模型选项，增加恢复笔记推荐参数按钮；长课输出预算明显不足时在请求前提示，截断错误显示本次实际预算。
- 语音接口个别块返回空文字时继续处理，原文标明无法识别的时间段并显示提醒；整节课均无文字仍报错，允许下次重新请求。
- 笔记仍按完整原文一次生成，添加跨进程缓存、配置交互和空识别块的回归测试。

## 0.5.0 — 2026-09-16

- 移除 MLX、CPU、CUDA 本地语音后端和相关依赖，改用可配置的云端音频上传接口。
- 默认硅基流动 XingChenASR-V3.2-Ultra；地址、密钥、模型、语言提示及返回格式均可配置，密钥保存在钥匙串。
- 长音频压缩后分块上传，尽量在停顿处切分；成功结果可恢复，切换模型或源文件会隔离缓存。
- 限流和临时故障有限重试，上传或等待时可取消；不向日志输出服务返回的原始错误或密钥。
- 支持 JSON 文字、带时间戳 JSON、纯文本和 SRT；没有完整真实时间戳时只保存文字，旧字幕隐藏归档。
- 原有转录可继续用于笔记，整课一次总结不变。更新设置、安装依赖、使用文档和离线回归测试。

## 0.4.0 — 2026-09-15

- 修正填写信息时设置区过矮的问题：开始前收起空白进度内容，设置与进度之间可拖动调整高度，移除设置区固定高度上限。
- App 按课程分组、按课次显示下载 / 本机转录 / 笔记状态，支持多门课程同时跟踪，运行中收起任务设置。
- 下载显示真实字节进度，转录显示已处理音频比例；笔记显示实际模型、请求次数、等待时间和重试倒计时，不虚构完成百分比。
- 总进度区分成功、失败、排队、等待回放、停止与意外中断；部分失败或停止时保留成功数量。
- 关键日志只记录状态变化，支持按课次筛选和暂停自动滚动；诊断默认折叠，可复制问题摘要、导出日志及查看最近一次运行摘要。
- 任务诊断日志按大小轮转、清理七天前的旧日志；原生启动日志在重新启动时按大小轮转。输出前隐藏凭据与带认证信息的链接。
- 增加“仅重试失败课次”和打开选中录像 / 笔记；按课程和课次精确选择，复用可用的录像、转录和完整响应缓存。
- 使用有版本、运行 ID 和顺序号的独立进度事件，旧运行和迟到事件不再覆盖当前状态；停止时等待后台进程清理。
- 笔记继续采用整课一次总结，正常成功仍仅一次 API 请求；本次验证使用模拟课程与模型，不产生付费调用。

## 0.3.2 — 2026-09-11

- 将脚本启动器替换为原生 Mach-O App 入口，在 App 进程中运行 GUI，使 macOS 能识别 iCourse 的文件访问权限。
- App 先检查保存目录，再启动后台课程任务；已保存的路径在重开时继续使用。
- 安装器在更新运行环境前编译入口，完整构建并本地签名后替换 App；支持 uv 与旧 Homebrew Python 运行环境。
- 添加连续启动、App 身份、后台运行环境及设置恢复测试。测试不读取真实凭据或调用 API。
- 升级后可能需要重新授权一次；普通重开不会重新构建 App。未来重新构建或重装仍可能触发系统授权，本地签名不是 Developer ID 签名或公证。

## 0.3.1 — 2026-09-11

- 在登录、下载、转录和笔记请求之前检查需要使用的保存目录，检查实际读取与写入权限。
- macOS 拒绝访问时显示具体目录及 Files & Folders 授权步骤，避免直接抛出 traceback。
- “只下载课程”不再扫描或迁移笔记目录，防止无关的 Documents 权限阻止下载。
- 系统隐私权限仍由用户授权；程序不自动修改权限或移动已有资料。

## 0.3.0 — 2026-09-09

首次整理公开的 Mac 桌面版。

- Apple Silicon 使用 MLX / Metal 在本地转录；保留 CPU 备用后端。
- 提供 Mac 安装器、独立运行环境和 Finder App 启动器；新安装使用 uv 管理的 Python 3.13。
- 提供 Qt 中文界面，账号密码和 API Key 保存至 macOS Keychain。
- 视频、字幕与文字笔记可以分别保存到外置磁盘和内置磁盘。
- 整课完整原文一次生成 Markdown 笔记，不分章调用，不额外生成提纲、复核或改写。
- 检查空响应、截断及异常结束；临时故障有限重试，成功结果缓存防止保存失败后重复调用。
- 复用已有转录可单独重新生成笔记；内部 JSON、恢复缓存和旧版笔记放入隐藏目录。
- 加入安装、使用、故障排查、开发文档和不使用真实凭据的自动化测试。
- 移除主分支中的旧云端工作流、网页端与未使用字体，保留整理前的历史标签。

当前未提供签名、公证的独立 DMG；没有为桌面版加入定时订阅、PPT OCR 或邮件推送界面。
